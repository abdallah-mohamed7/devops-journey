
name = "devops"
version = 3.11

print(f"Python {version} for {name}")

servers = ["ai1", "web2", "database"]
for server in servers:
    print(f"Checking {server}...")
for server in servers:
      if server == "database":      
              print(f"we find it {server}")
      else:
             print(f"data base not find ")
if "database" in server :
 print(f"we find it")

