

name: Deply hadith API


on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: checkout code 
          uses: actions/checkout@v3 
        - name: print confirmation 
          run: echo "Hadith API workflow triggered successfully"
